/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
    domains: ['images.unsplash.com'], // Ajoutez les domaines pour vos images
  },
}

module.exports = nextConfig
